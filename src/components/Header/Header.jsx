import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.headerCont}>
      <div>Meet up</div>
      <div>Search</div>
    </div>
  );
};

export default Header;
